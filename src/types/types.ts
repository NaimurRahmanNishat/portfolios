// navLinks types
export interface NavLinks {
  name: string;
  path: string;
}

// stats types
export interface StatProps {
  num: number;
  postfix: string;
  text: string;
}

// service types
export interface ServiceProps {
  id: string;
  title: string;
  description: string;
  href: string;
}
